import { pageMetadata } from "@/lib/search";

export const metadata = pageMetadata("/data-deletion");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
